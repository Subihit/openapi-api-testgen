openapi: 3.0.0
info:
  title: Simple API
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      responses:
        "200":
          description: OK

    post:
      operationId: createUser
      responses:
        "201":
          description: Created
  /users/{id}:
    get:
      operationId: getUser
      responses:
        "200":
          description: OK
